# SISP AppHub + MapDrive — Live Deploy Log

Append-only journal of every action taken against the live cluster, with verification and a
rollback for each. Newest at the bottom. Hard constraints (owner-set): **(1) never delete NAS
files, (2) never change NAS appliance settings, (3) never down node1–4 or break the live apps**
(jupyterhub, rstudio, galaxy, zulip, leantime, …, and the existing `:8792` apphub scaffold).

Strategy: additive/isolated/reversible steps run now; inherently-disruptive flips (slapd schema
load, locker chown/chmod, nginx public cutover, scaffold stop) are GATED on an explicit go.

Branch: `feat/apphub-rebuild`. Each step is committed; milestones tagged.

| # | when (UTC) | step | type | verified | rollback |
|---|---|---|---|---|---|
| 0 | 2026-06-27 | baseline tag + health snapshot | read-only | — | n/a |
| 1 | 2026-06-27 | `ldapadd` role groups `apphub-power`(100100), `sisp-admins`(100200) under ou=group | additive LDAP | authoritative LDAP read shows both w/ memberUid=kriengkraip; slapd active; getent user OK | `ldapdelete -x -D cn=admin,dc=siriraj,dc=local -w … cn=apphub-power,ou=group,dc=siriraj,dc=local cn=sisp-admins,ou=group,dc=siriraj,dc=local` |
| 2 | 2026-06-27 | add `cn=apphub-ro` bind (additive); deploy **Authelia** container on loopback :9091 (`/opt/sisp-apphub/auth`, host-net, own sqlite/volume) | additive/isolated | container healthy; wrong-pass→KO, `kriengkraip/heng`→OK vs **live LDAP**; groups resolve sisp+apphub-power+sisp-admins; **health re-check == baseline** (only :9091 added) | `cd /opt/sisp-apphub/auth && docker compose down -v`; `ldapdelete … cn=apphub-ro,dc=siriraj,dc=local` |
| 3 | 2026-06-27 | deploy backend (systemd `apphub-backend`, loopback :8793, postgres db `apphub`) — header-trust auth, real SLURM reads, **nginx-reload disabled** | additive/isolated | healthz OK; spoofed header→authenticated:false, proxy-secret→admin role; real sinfo (112c/node); reads live squeue (logs untracked job, doesn't touch it); health==baseline | `systemctl disable --now apphub-backend`; `sudo -u postgres dropdb apphub` |
| 4 | 2026-06-27 | **PUBLIC CUTOVER**: swap `apphub.sisp.com` vhost → new SPA (`/opt/sisp-apphub/web`) + Authelia in-app auth + backend :8793 (scoped to 1 conf; backup + `nginx -t` + verify + auto-rollback) | flip (reversible) | apphub/=200 (new SPA), /api/session 401→login; **E2E over TLS**: kriengkraip/heng→authenticated admin, real sinfo; wrong pw rejected; **zulip/leantime unchanged** | `cp /etc/nginx/conf.d/43-apphub-sispcom.conf.bak-<ts> 43-apphub-sispcom.conf && nginx -t && systemctl reload nginx` (restores scaffold :8792/:8888) |
| 5 | 2026-06-27 | feature update + redeploy (dist + backend): configurable/clickable template cards + favorites (star); **batch-job** catalog entry; **Job Queue shows all the user's real SLURM jobs** (live squeue merged, owner-scoped) | additive redeploy | batch-job in templates; admin sees real job 2470 (ryanr's kmer), ryanr sees own, krittiyabhornk sees [] (no leak); apphub/=200; health==baseline | redeploy previous `dist`+backend from tag `deploy-apphub-live` and `systemctl restart apphub-backend` |
| 6 | 2026-06-27 | `reconcile-identity.sh --new-only --apply`: provision the **16 missing** user lockers (empty, LDAP-owned, private 2700) | additive | 16 created, owner=LDAP uid, owner-only; **ryanr's 27 running jobs unaffected**; services + apphub healthy | `sudo bash /var/log/apphub/identity-rollback-20260627-182105.sh` (rmdir empties) |
| 7 | 2026-06-27 | `reconcile-identity.sh --apply` (owner-approved despite busy cluster): **chmod 46 lockers → private 2700**, **chown 8 stranded → LDAP uid** (dianap 1005→10012, thanaphonl 1011→10032, …); shadow removals **emitted, NOT run** | flip (reversible) | lockers owner-only + correct uid; ryanr 25 jobs **0 FAILED/CANCELLED**; slapd/nginx/pg/slurm/apphub healthy; ldap OK | `sudo bash /var/log/apphub/identity-rollback-20260627-192701.sh` |
| — | DEFERRED | shadow-account `deluser` (13, incl. ryanr) | disruptive | held while users have live jobs (removing an account under running processes corrupts them); run `/var/log/apphub/remove-shadows-<ts>.sh` per-user when each user is idle | re-add account |
| 8 | 2026-06-27 | validate **launch-as-user** path live (sbatch as kriengkraip via sudo→runuser) | test job | job 3072 **COMPLETED exit 0**: ran as **kriengkraip uid=10000 on node4**, wrote to private locker (owner 10000), **nodeadmin cannot read** (isolated). Privilege boundary + locker isolation proven on reconciled lockers | test artifacts left in user locker (no-delete constraint) |
| 9 | 2026-06-27 | **LAUNCH SUBSYSTEM live**: built python-apps.sif (NAS), installed sbatch wrapper + runner (shared NAS bin) + sudoers (NOPASSWD, scoped, visudo-OK), self-signed `*.app` cert, `*.app.sisp.com` nginx vhost (routes-map + Authelia + cookie-strip), reconciler writes routes.upstreams + reloads nginx | additive + flip | **end-to-end app launch works**: launch static-html as kriengkraip → runs in container on node2 as uid 10000 → route published → reachable directly (200) and via nginx→Authelia→node2 (authed 200, unauth 302); other vhosts unaffected | `rm /etc/nginx/conf.d/44-apphub-apps.conf && nginx -t && systemctl reload nginx`; `rm /etc/sudoers.d/apphub` |
| 10 | 2026-06-28 | **feature batch + cancel fix** redeploy (backend src + dist + scripts): launch **folder path** (`--folder`→`--chdir` workspace), **team sharing** (`canSee` team + `?scope=shared`), **Job Queue cancel** (own; admin multi-select) via new `POST /api/jobs/:id/cancel`, **Workspace file browser** (`GET /api/files`→`sudo apphub-fileop.sh`→runuser, read-only), **Admin** multi-select/per-row stop, Dashboard "Shared with you". **FIX**: stop/cancel now `sudo scancel` (was nodeadmin scancel → couldn't kill other users' jobs → orphans held ports → `Errno 98` launch collisions). sudoers += `scancel`,`apphub-fileop.sh` (visudo-OK); `.env` += `APPHUB_FILE_HELPER` | additive redeploy + flip | dist tsc-clean; backend active; **file browser lists kriengkraip real locker**; team app GET as other user→200; **owner cancel→204 kills job**; fresh launch→running→app stop→**job gone from squeue (sudo scancel verified)**; other vhosts/ryanr jobs untouched | redeploy prior `dist`+backend from tag `deploy-launches-live`, restart |
| — | NOTE | pre-fix orphan job `3078` (apphub-demo, kriengkraip) still RUNNING on node2 (instance already marked stopped in store, so app won't re-cancel) | cleanup | harmless: self-expires at 120-min timelimit; new launches get unique ports (verified no collision). User can clear via new Job Queue **cancel** button (owner) or `! plink … scancel 3078` | n/a |
| 11 | 2026-06-28 | **MapDrive web restyle live**: rebuilt `mapdrive.sisp.com` on the AppHub design language (teal `#11695f` token system, Inter + JetBrains Mono, Remix Icon), wired Codex assets (wordmark, hero bg, ambient light/dark bg, 3 how-it-works spots, OS glyphs, logo), added How-it-works + footer + **light/dark toggle**. Static deploy to `/var/www/mapdrive.sisp.com` (index.html, styles.css, app.js, assets/ only — `config/`+`downloads/` untouched); backed up first; no service restart (static) | additive (static) | served title = "SISP MapDrive…"; styles/wordmark/hero = 200; **`config/share-presets.json` preserved (200)**; public TLS `https://mapdrive.sisp.com` = 200 w/ new page + assets; `sisp-mapdrive.service` unchanged | `cp -a /var/backups/mapdrive-web-20260628/* /var/www/mapdrive.sisp.com/ && chown -R www-data:www-data /var/www/mapdrive.sisp.com` |
| 12 | 2026-06-28 | **MapDrive exe restyle + repackage**: restyled `SISPDriveMapper.ps1` onto AppHub tokens (brand teal palette, brand-teal primary Connect button, branded tray icon + green/amber status dots, Consolas mono for the UNC list + activity log), branded macOS `.command` header. Rebuilt installer (csc + Inno Setup) embedding new `app-icon.ico`; deployed Setup.exe + portable ZIP + macOS ZIP to live `downloads/` (backed up first) | additive (static) | PS1 parses clean (Parser, 0 errors); build OK; `downloads/{Setup.exe,zip,macOS.zip}` = 200 (5.37M/3.36M/2.5K) over :8791 | `cp -a /var/backups/mapdrive-web-20260628/downloads/* /var/www/mapdrive.sisp.com/downloads/` |
| 13 | 2026-06-28 | **AppHub file manager + approvals + select-all**: new multi-op `apphub-fileop.sh` (list/stat/mkdir/mkfile/rename/delete/read/write/hash — all via `runuser` as the user, atomic temp→rename, sha256 + size verified, streaming); backend file-op routes + streaming up/download; real approvals store (`approvals.js`) + endpoints (create/list/approve/deny, grants `power`); Workspace rebuilt as a file manager (toolbar, multi-select, upload progress + client/server hash, text preview); JobQueue + Admin select-all; Catalog "Request hosting". Redeploy backend src + dist + helper (sudoers `apphub-fileop.sh *` already allowed) | additive redeploy | dist tsc-clean; helper syntax OK; **live**: mkdir/mkfile/upload/list/hash/download/rename/delete all 2xx, download body correct, **wrong-hash upload→422**, **traversal→400**, test artifacts cleaned; approvals create→admin-sees→researcher-403→approve→requester power=true; SPA bundle live; other apps/ryanr jobs untouched | redeploy prior dist+backend from tag `mapdrive-exe-restyle`; restore prior `apphub-fileop.sh` |
| 14 | 2026-06-28 | **Fix Workspace 400 + extend/clear/custom-templates/labels**: loosen file-path validation (allow real filename chars — `( ) , &` etc.; only `..`-segments + control chars blocked; security is `runuser`+realpath) and fix `confined('')` root case; self-service **extend/top-up** (real `scontrol update job TimeLimit`, +12h step, 7-day cap) + template max times raised to 3 days + longer launch presets; **Clear finished** (admin prune terminal) + **COMPLETING→"completed"** label; **custom catalog templates** (admin shared / user personal, `custom-templates.js`) + New-template modal + manage/delete; batch GONE→"finished" not failed. sudoers += `scontrol update *` | additive redeploy + flip | **live**: root + special-char folders list 200; full catalog matrix run as admin+researcher, private+team (static-html/streamlit/fastapi→running; jupyter*/rstudio* need their `.sif`; batch→finished); team GET 200; extend 120→840 (scontrol); custom template create/list/launch→running/delete + non-admin shared→403; clear-finished prunes; researcher clear→403; all test jobs cleaned; ryanr/other apps untouched | redeploy prior dist+backend from tag `apphub-filemanager`; restore `/etc/sudoers.d/apphub.bak-20260628` |
| — | TODO | build heavier images `jupyterlab.sif` + `rstudio.sif` on the NAS (`/mnt/sisplockers/.apphub-images/`) so the 4 notebook/RStudio templates launch (currently only `python-apps.sif` exists → those 4 fail "image missing"). streamlit/gradio/fastapi need a real entry file in the user's locker | follow-up | n/a | n/a |
| 15 | 2026-06-28 | **AppHub** lazy folder sizes (`dirsize` op/endpoint, 6s `du` guard) + delete on custom template cards; redeploy backend+helper+dist | additive redeploy | live: `.config`=23KB, `.cellpose`=26MB, huge trees→"—"; SPA bundle live | redeploy from tag `apphub-extend-templates` |
| 16 | 2026-06-28 | **MapDrive ground-up redesign**: web (`/var/www/mapdrive.sisp.com` index/styles/app — tool-first single page, dropped Tailwind CDN, light/dark, share table) + exe UI rebuild (rebranded "SISP MapDrive", new layout, logic preserved) + installer rebuilt; static deploy, `config/` preserved | additive (static) | web TLS 200 (connect panel, no Tailwind, hero present); downloads 200; PS1 parse-OK; `sisp-mapdrive.service` untouched | `cp -a /var/backups/mapdrive-web-20260628b/* /var/www/mapdrive.sisp.com/` (web) and `/…/downloads/*` (installer) |
| 17 | 2026-06-28 | **Perf**: confirmed apphub app-proxy already optimal (http2, ws upgrade, 3600s read, buffering off) + global gzip/keepalive on; added **persistent pip cache** to runner (`--bind pipcache --env PIP_CACHE_DIR`) so repeat streamlit/fastapi launches reuse wheels; main-app vhosts left untouched (working prod, app-level slowness) | runner update (NAS) | static-html still →running (new `--env`/`--bind` accepted, launch path intact); `pipcache` 1777 created | restore prior `apphub-runner.sh` from tag `mapdrive-redesign-v2` |
| 18 | 2026-06-28 | **CIFS gateway groundwork** (auth model chosen: ldapsam + self-serve drive password): `slapcat` backup of LDAP (config 667 + data 1337 lines → `/root/ldap-*-backup-20260628.ldif`); installed Samba 4.15.13 on node1; **verified `ldapsam` backend is viable** (compiled-in LDAP support; `testparm` accepts `passdb backend=ldapsam` + the `[sisplockers]` share). Auto-started unconfigured `smbd`/`nmbd` **disabled** (445/139 closed) so no half-configured SMB on the LAN | additive (reversible) | smbd/nmbd inactive, 445 closed; **slapd active + getent + jupyter 302 unaffected** throughout; LDAP untouched (backup only) | `apt-get remove --purge samba` (foundation only; no LDAP writes made) |
| 19 | 2026-06-28 | **gateway de-risked + scripted**: **S1 ownership gate GREEN** on node1 (writing as uid 10000 → owner 10000:100000 preserved on `/mnt/sisplockers`); Samba LDAP schema located locally (`/usr/share/doc/samba/examples/LDAP/samba.ldif` — no fetch needed); spotted that the existing `{2} to * by * read` ACL would expose NT hashes → runbook adds a protecting rule. Authored one-command completion script `gateway/install-gateway-node1.sh` + `gateway/smb.conf.node1` (local ldapsam, `/mnt/sisplockers/%U`, per-user isolation). Authored `apphub/images/build-images.sh` (python-apps pre-bake + jupyterlab + rstudio). **Auto-mode classifier BLOCKED the live `ldapadd` schema load into prod LDAP** — correct: cluster-wide-auth blast radius needs a watched/approved run | groundwork + scripts | S1 GREEN; schema present; node1 clean (smbd disabled); LDAP untouched (backup intact) | n/a (no live LDAP write made) |
| 20 | 2026-06-28 | **CIFS gateway LIVE on node1** (user-run, outside auto mode): `install-gateway-node1.sh` (schema + pwsync bind + NT-hash-protecting ACL + smb.conf + enable smbd) then `fix-gateway-domain.sh` (created the `sambaDomainName=NAS` entry + domain SID `S-1-5-21-361032565-262258231-9256875` that ldapsam needs; wired `APPHUB_SAMBA_DOMAIN_SID`). **smbd active, 445 open** (ufw inactive/iptables ACCEPT — no fw change). End-to-end verify (drive-password + smbclient owner check) is the user's self-serve step (`/tmp/gw-verify.sh`, prompts for creds) | flip (reversible) | smbd active; slapd/apphub/jupyter unaffected; LDAP backed up | `systemctl disable --now smbd nmbd`; `ldapdelete cn=samba-pwsync,…`; restore olcAccess |
| 21 | 2026-06-28 | **node1 parity + MapDrive gateway-default**: installed **apptainer 1.5.2** on node1 (`ppa:apptainer/ppa`) + `singularity` symlink so node1 matches the compute nodes (build+run). MapDrive web rebuilt with a **gateway/direct mode toggle** (Recommended = node1 gateway `192.168.0.25`, plain username, ownership-correct; Direct = Infortrend `192.168.0.103`, dept/project shares) — `config={modes:{...}}`, default=gateway; deployed live (prior backed up). **Image builds running on node1** (atomic build-images.sh: python-apps pre-baked + jupyterlab+scanpy + rstudio) | additive + flip | apptainer 1.5.2 OK; mapdrive TLS 200, modes live; core services up; build pulling at ~45MB/s | restore `/var/backups/mapdrive-web-20260628c/*`; `apt remove apptainer` |
| 27 | 2026-06-28 | **hosted-app fixes + folder zip download + JupyterHub spawn fix**: (a) hosted JupyterLab XSRF fixed (`--ServerApp.disable_check_xsrf` etc.) so terminals/workspaces work behind the proxy; (b) jupyterlab.sif rebuilt with **ipywidgets 8.1.8** (+notebook/seaborn) → 674M; (c) **Workspace folder zip-download** (new `zipdir` op streams `zip -qr -` as the user → `GET /api/files/zip`; folder rows + multi-select download zip — verified valid archive, non-dir→400); (d) **main JupyterHub spawn fixed**: `c.Spawner.http_timeout=120`/`start_timeout=300` (was failing the 30s default on slow single-user startup) + disabled broken `jupyterlab_tensorboard_pro` ext; restarted (verified NOT caused by the 775 chmod — jupyter starts fine under group-writable home). Conda = option A (main hub on node1 has all envs) | additive + main-app config | jupyterlab+rstudio launch; zip 200 valid; jupyterhub 302/login 200, timeouts live | restore `jupyterhub_config.py.bak-*` + restart; redeploy prior runner/dist |
| 28 | 2026-06-28 | **shared + personal conda envs as Jupyter kernels** (user request): rebuilt `jupyterlab.sif` on a **miniforge** base — conda+mamba+**nb_conda_kernels**+ipywidgets 8.1.8+scanpy/anndata/leidenalg/harmonypy (661M, atomic swap). Runner jupyter case binds the shared pool (`/mnt/sisplockers/.apphub-conda`→`/opt/shared-conda` **ro**) + writes a managed `~/.condarc` with `envs_dirs` = `/workspace/.conda/envs` (personal, in-locker) + `/opt/shared-conda/envs` (shared) + `--ServerApp.kernel_spec_manager_class=nb_conda_kernels…`; `run()` gained `EXTRA_BINDS`. **Helpers**: `apphub-conda` (in-container, on PATH — `apphub-conda new <name> [pkgs]` makes a personal env w/ ipykernel+ipywidgets) + `apphub-conda-admin` (node1, sudo — creates SHARED envs into the pool, world-readable, created at the same `/opt/shared-conda` prefix the runner binds so kernels resolve). Catalog descriptions updated; backend restarted | additive (NAS image+runner+helpers) + backend restart | **live E2E (kriengkraip)**: sif build prints `ipywidgets 8.1.8`; seeded SHARED `nextflow` (+nextflow binary+ipykernel) and PERSONAL `nf-heng` (owned `kriengkraip:sisp`); launched jupyterlab → running (node2) → `GET /api/kernelspecs` lists **base + `conda env:shared-conda-nextflow` + `conda env:.conda-nf-heng`**; `apphub-conda list` shows both | restore prior `apphub-runner.sh` + `jupyterlab.sif` from `/mnt/sisplockers/.apphub-images/*.bak-*`; `rm -rf /mnt/sisplockers/.apphub-conda`; `rm /usr/local/sbin/apphub-conda-admin` |
| 29 | 2026-06-28 | **Performance report** (user request): measured TTFB+total over the live nginx edge (25 samples/endpoint) from node1; built `docs/performance-report.html` (self-contained, interactive — KPIs, sortable stats table, avg/p95/TTFB toggle bar chart, light/dark) and deployed to `https://mapdrive.sisp.com/performance-report.html`. Finding: new hosting/gateway is **faster at the HTTP layer** than legacy (hosted edge ~7.9ms vs Jupyter ~23ms / RStudio ~17ms; gateway+Authelia overhead ~2ms; AppHub API ~0.65ms). **Galaxy returned 502** (upstream down — flagged for health check) | additive (static) | report 200 over TLS; 200 samples embedded | `rm /var/www/mapdrive.sisp.com/performance-report.html` |
| 30 | 2026-06-28 | **hosted-app parity: real-path terminal + RStudio fixed + hosted-vs-main dashboard + public-env mirror** (user requests): (a) runner mounts the locker at its REAL path for jupyter+rstudio (new `WS_MOUNT`) so the JupyterLab terminal `pwd`/HOME = `/mnt/sisplockers/<user>` like jupyter.sisp.com and personal envs sit at `~/.conda/envs` (verified: jupyter `root_dir=/mnt/sisplockers/kriengkraip`, nb_conda_kernels 3 kernels). (b) **RStudio launch FIXED** — root cause: node1 apptainer injects the LDAP user but the compute nodes' older singularity does NOT, so the synthetic `appuser`+grep-guard left `--server-user` unresolvable; now uses the REAL user (host-resolved, injected if absent) + binds writable host dirs over `/etc/rstudio`,`/var/lib/rstudio-server`,`/run/rstudio-server` (old singularity can't create files in container `/etc` → `database.conf: Permission denied`) + sqlite `database.conf`. (c) **unpublished** the perf report from mapdrive. (d) **hosted-vs-main comparison** (30 samples/endpoint, live edge) → `docs/performance-dashboard.html` (responsive desktop+mobile, interactive). (e) **public conda-env mirror** started (cp+sed relocation, background, throttled) | additive (runner) + static report | jupyter real-path+kernels ✓; **rstudio serves 302** (node3); dashboard 180 samples; mirror running | restore prior `apphub-runner.sh` from `/mnt/sisplockers/.apphub-images/bin/*.bak-*`; `rm docs/performance-dashboard.html` |
| — | RESULT | hosted vs main (avg total, ms): **Jupyter** main 23.5 → hosted-edge 7.3 (**3.2× faster**) → raw app 1.0; **RStudio** main 17.2 → hosted-edge 7.7 (**2.2× faster**) → raw app 1.1. Gateway+SSO overhead ~6ms; all endpoints 302, measured identically | measurement | n=30 each | n/a |
| — | INPROG | public shared conda envs (user-specified): ashlar, cp4, denv_genome, denv_genome_supplementary, dr_folk, nextflow(done), palom, python3 — mirrored from `/opt/miniconda3/envs` into `/mnt/sisplockers/.apphub-conda/envs` via cp+prefix-rewrite (`/tmp/env-mirror.log`). conda-pack abandoned (hub pkgs cache cleaned → FileNotFoundError) | background | log = `/tmp/env-mirror.log` | `rm -rf /mnt/sisplockers/.apphub-conda/envs/<name>` |
| 31 | 2026-06-28 | **hosted-app UX fixes** (user scenarios): (a) **JupyterHub 403 XSRF** = client-side stale `_xsrf` cookie (cookie_secret stable since 2023; a server-side login probe with dummy creds passes XSRF → "Invalid username or password"). Fix for users: clear cookies / incognito. (b) **JupyterLab terminal parity**: login+interactive bash (`terminado_settings shell_command=['/bin/bash','-l']`) + USER/LOGNAME/SHELL + HOME=locker, so the Terminal sources `~/.bash_profile`/`.profile`/`.bashrc` and `pwd`=/mnt/sisplockers/<user> like jupyter.sisp.com. (c) **RStudio redirect loop FIXED** (`ERR_TOO_MANY_REDIRECTS`): under `--containall`, USER/LOGNAME are empty → `auth-none` signs in an EMPTY user → malformed secure cookie → `/`↔`/auth-sign-in` loop; AND the `*.app` vhost stripped ALL cookies. Fix: set USER/LOGNAME; stop masking `/etc/rstudio` (single-file-bind a readable `database.conf`; `rsession-which-r`; `www-verify-user-agent=0`; whole `/run`+`/var/lib/rstudio-server` binds); nginx `*.app` strips **only** `authelia_session` (map) so the app keeps its own session cookie. (d) **unique app names**: launch auto-uniquifies the slug (`uniqueName`) so two unnamed apps don't collide on `name-owner.app` (a collision also emits a duplicate nginx map key → reload fails); routes-table dedups hosts as a backstop. (e) RStudio **session persistence** for long jobs (haymaro/Seurat): HOME + `server-data-dir` + secure-cookie key all in the locker → the R session/`.RData`/open files survive stop/timeout and auto-restore on relaunch; combine with the +12h extend | runner + backend + nginx (reversible) | jupyter launches+kernels OK w/ new flags; 2 unnamed rstudio → `rstudio-2`/`rstudio-3` distinct URLs, `nginx -t` OK; **rstudio GET / → 200 (IDE), no loop** | restore `apphub-runner.sh`,`server.js`,`validate.js`,`routes-table.js` from `*.bak-*`; restore `/etc/nginx/conf.d/44-apphub-apps.conf.bak-*` + reload |
| 32 | 2026-06-28 | **cross-folder access for hosted apps** (user scenario: read `sisplockers/ryanr`, use `Rarecyte-folder` data): runner refactored (`WS_SRC`/`WS_DST`/`WS_PWD`) so JupyterLab/RStudio bind the **whole `/mnt/sisplockers` tree** at its real path (chdir into the user's own locker) — users can read/write other lockers exactly where the 0775 + sisp-group perms allow (verified: container sees 71 lockers). Plus `bind_data_shares` binds research/data dept shares (`rarecyte-folder, MutationProfile, CRCproject, research, Aj-Adisak`) at `/mnt/<name>` **when present on the node** (admin/HR/finance shares excluded). **FIX**: the share-absent case in an `&&`-chain returned non-zero under the runner `set -e` → aborted the launch (empty job log) on compute nodes (which lack the shares); rewrote with `if`. Authored `apphub/images/mount-data-shares.sh` (staged at `/mnt/sisplockers/.apphub-images/bin/`) to NFS-mount those shares on node2-4 (they're node1-only today) | runner (NAS) + delivered mount script | jupyter launches+kernels OK with tree bind; cross-locker read verified (71 lockers); data shares auto-bind once mounted | restore prior `apphub-runner.sh` from `*.bak-*`; `umount`+fstab-remove per the script footer |
| — | USER-RUN | **enable dept data shares in hosted apps**: classifier (correctly) blocked auto-SSHing into compute nodes to edit fstab/mount. Run from node1 as root: `for n in node2 node3 node4; do ssh "$n" 'bash /mnt/sisplockers/.apphub-images/bin/mount-data-shares.sh'; done`. Cross-locker (`sisplockers/*`) already works with no mount needed | follow-up | n/a | n/a |
| 33 | 2026-06-28 | **all NAS shares mounted cluster-wide + Seurat image** (user-directed "mount everything like node1"): replicated **all 14 of node1's NAS (192.168.0.103) shares** onto node2/3/4 via fstab (`nfs4 _netdev,nofail,vers=4.2`; list generated to `/mnt/sisplockers/.apphub-images/bin/dept-shares.fstab`, applied by `mount-data-shares.sh`) — every dept/project share now mounted at `/mnt/<name>` on every node (verified **ok on node2/3/4**). Runner `bind_data_shares` rewritten to **auto-detect** every Infortrend `/mnt/*` mount and bind it into JupyterLab/RStudio (excludes the lockers tree + galaxy-app; OS perms enforce who can read each), so whatever is mounted just appears in apps. **rstudio-seurat** now uses a dedicated `rstudio-seurat.sif` (rocker + Seurat + harmony + presto; new build-images.sh recipe), with a graceful fallback to plain `rstudio.sif` until it's built; image building on node1 (~30-60 min) | infra (compute-node mounts, additive/nofail) + runner + image build | 14 shares mounted+verified on node2-4; runner auto-binds; seurat build running | per node `umount /mnt/<s>` + remove its fstab line; restore `apphub-runner.sh` from `*.bak-*` |
| 34 | 2026-06-28 | **hosted Terminal conda parity + file-browser root + Seurat deps** (user): (a) **terminal conda fixed** — users' `~/.bashrc` (shared with jupyter.sisp.com) runs `source /opt/miniconda3/bin/activate; conda activate python3`, but the container's conda is `/opt/conda` → "No such file or directory" + bare `$`/`Apptainer>` prompt. Baked a **`/opt/miniconda3 → /opt/conda` symlink** into `jupyterlab.sif` (fast `localimage` rebuild + the build-images.sh recipe) so the legacy path resolves and the Terminal initialises conda like the main hub. Verified inside the container: **no errors**, `who=kriengkraip`, `pwd=/mnt/sisplockers/kriengkraip`, conda python on PATH, `env=base` (→ `(python3)` once the python3 shared env finishes copying — it activates that env). (b) **file-browser root = the locker** (verified: `root_dir=$WS`; `/api/contents` path `''` lists the locker's own files; JupyterLab renders that root as `/`). (c) **rstudio-seurat build** first failed (`fs` R pkg needs libuv) → added `libuv1-dev libgit2-dev libsodium-dev libudunits2-dev zlib/bz2/lzma -dev` to the recipe; rebuilding | image rebuild + recipe | terminal conda OK (verified); browser root = locker (verified); seurat rebuild running | restore prior `jupyterlab.sif` from `/mnt/sisplockers/.apphub-images/.build-*`/backup |
| 35 | 2026-06-28 | **file-browser root=/ + conda in RStudio terminals + env mirror complete** (user): (a) JupyterLab `--ServerApp.root_dir=/` + `--preferred_dir=$WS` — the file browser can navigate the whole bound tree (other lockers + data shares, with real absolute paths) but opens in the user's locker (verified: `/api/contents` root lists the container root incl. `/mnt`). (b) **conda in the RStudio Terminal**: installed a small **miniforge on NFS** (`/mnt/sisplockers/.apphub-conda/base`, conda 26.3.2, reached via the lockers-tree bind at its real install path → prefix-correct) + baked `/opt/miniconda3 → that` into `rstudio.sif` (+ the rstudio-seurat recipe + a background finalizer for the in-flight seurat build); runner binds the shared env pool at `/opt/shared-conda` and writes `~/.condarc` for rstudio too. **Verified**: rstudio terminal → no errors, `(python3) kriengkraip@…`, python from `/opt/shared-conda/envs/python3`. (c) **conda env mirror ALL DONE** — 8 public envs (ashlar, cp4, denv_genome, denv_genome_supplementary, dr_folk, nextflow, palom, python3). build-images.sh updated for reproducibility | image symlink + NFS miniforge + runner | jupyter root=/ (locker default); rstudio+jupyter terminals `(python3)`; mirror complete | restore prior `rstudio.sif`/`jupyterlab.sif` from `.build-*`; `rm -rf /mnt/sisplockers/.apphub-conda/base`; runner from `*.bak-*` |
| 36 | 2026-06-29 | **jupyter terminal prompt fix + unlimited runtime** (user): (a) **terminal prompt** — apptainer sets a one-shot `PROMPT_COMMAND='PS1="Apptainer> "; unset PROMPT_COMMAND'` that overwrote the prompt AFTER `.bashrc` ran (PTY-probe confirmed), so the terminal showed `Apptainer> ` despite conda being active. Fix: terminado launches `bash --rcfile /opt/shared-conda/bin/apphub-term-rc`, which sources `~/.bashrc` then `unset PROMPT_COMMAND` → prompt is `(python3) kriengkraip@<node>:~$` (verified). (b) **unlimited runtime**: LaunchWizard gains an "Until I stop it" option (`timeMinutes=null` → SLURM `UNLIMITED` partition, already supported by the wrapper); built+deployed the SPA. **Incident+fix**: a `pscp -r` of the dist silently transferred nothing while the deploy had already `rm -rf web/assets` → AppHub SPA assets briefly missing; restored via a tarball deploy of the new build (bundle `index-CXS12iWu.js`, SPA+bundle 200) | runner + frontend build | terminal prompt verified; unlimited app ran (timeLimitMinutes=None); SPA assets restored | restore `apphub-runner.sh` from `*.bak-*`; restore `web` from `/opt/sisp-apphub/web.bak-*` |
| 37 | 2026-06-29 | **vanity URLs + external :8443 + opt-in public apps** (user, "go ahead"): (a) **vanity** — `lib/vanity.js` store + endpoints (`/api/vanity` request/list, `/api/vanity/:n/decide` admin, DELETE); an approved vanity routes at `<name>.app.sisp.com` (no `-owner`); launch skips uniquify for an approved vanity. (b) **external** — `45-apphub-external.conf`: `apphub.sisp.freeddns.org:8443` (SSO, in-app login, mirrors the apphub vhost) + `*.sisp.freeddns.org:8443` (public apps, **anonymous**) on the existing LE `*.sisp.freeddns.org` wildcard cert. (c) **opt-in public** — a `public` flag on apps; `routes-table.js` now emits a 2nd map (`routes.public.upstreams`, host on publicDomain→upstream) for public+running apps; reconciler sets `publicUrl`; `POST /api/apps/:id/public` toggles it. Authelia: added a `sisp.freeddns.org` session-cookie domain + an `apphub.sisp.freeddns.org` one_factor rule (backed up, health-checked, auto-rollback guard). Postgres store keeps new fields (jsonb). | backend + nginx + Authelia (reversible) | **verified**: vanity `my-portfolio.app.sisp.com`; apphub.sisp.freeddns.org:8443 SPA 200 ×3 + /api/session 401 (SSO); public app `extpub-kriengkraip.sisp.freeddns.org:8443` **200 anonymous**; live apphub.sisp.com unaffected (SPA 200, session 401) | `rm /etc/nginx/conf.d/45-apphub-external.conf`+reload; restore `configuration.yml.bak-*`+restart authelia; redeploy prior backend `src/*` |
| — | USER-RUN | **router NAT**: point the ZYXEL `*.sisp.freeddns.org:8443` → node1:8443 (agent can't touch the router). Then external URLs work from outside: `https://apphub.sisp.freeddns.org:8443` (SSO) + public apps `https://<name>.sisp.freeddns.org:8443` (anonymous). | follow-up | n/a | n/a |
| 26 | 2026-06-28 | **lockers opened to 775 for collaboration** (owner-directed, reverses the step-7 0700 privacy): `set-locker-perms-775.sh` backed up current modes (`/root/locker-perms-backup-20260628-175823.txt`, 72 entries) then `chmod 0775` each `/mnt/sisplockers/<user>` dir → effective `drwxrwsr-x` (775 + inherited setgid keeps the `sisp` group). **Lab group (gid 100000 = all 46 members) can now read AND write every locker**; others read. Directory-level only (files keep their perms; 0600 files stay private). With the gateway `[sisplockers]` full-tree share, cross-user collaboration now works | flip (reversible) | 72 dirs → drwxrwsr-x; sample verified; backup saved | `while read m u g p; do chmod "$m" "$p"; done < /root/locker-perms-backup-20260628-175823.txt` |
| 25 | 2026-06-28 | **gateway locker share = full tree (collaboration)**: changed gateway `[sisplockers]` from per-user `%U` isolation to the whole `/mnt/sisplockers` tree (`valid users=@sisp`, browseable, masks 0644/0755) so users can read/write others' lockers where those lockers' own perms allow — exactly like the direct NAS share. Renamed MapDrive gateway entry "My locker" → "sisplockers"; gateway dropdown now mirrors direct naming. smbd reloaded; config live. NOTE: lockers reconciled to 0700 stay private by default — cross-user access happens only where a user opens their own locker (opt-in; no mass chmod done) | flip (reversible) | smbd active; `[sisplockers]` full-tree; live gateway shares = sisplockers + 13 | restore `/etc/samba/smb.conf.bak-locker-*` + reload smbd; restore prior config |
| 24 | 2026-06-28 | **gateway serves department/project shares too**: `add-gateway-shares.sh` mounted the 13 NFS-exported shares on node1 (admin_dept, admin_sp, Aj-Adisak, CRCproject, filing, hr, it_others, postgraduate, purchasing, research, undergraduate, MutationProfile, Rarecyte-folder; `_netdev,nofail` fstab) + added Samba stanzas (per-user identity, `valid users=@sisp`, OS perms enforce access like direct). smbd reloaded (no session drop). MapDrive gateway mode now lists locker + 13 shares; config deployed. The ~10 CIFS-only (not NFS-exported) shares stay Direct-only | additive (mounts + smb.conf) | testparm serves all 14 shares; `@sisp`=46 members; smbd active; config live (14 gateway shares) | `umount /mnt/<share>` + remove fstab lines + delete the auto block in smb.conf (backed up `smb.conf.bak-*`) + reload smbd; restore config from `share-presets.json.pre-gwshares` |
| 23 | 2026-06-28 | **GATEWAY VERIFIED END-TO-END** + drive-password wired: `fix-drivepw.sh` reset the `samba-pwsync` bind to a known password (lost when the install script aborted on first smbd-start under set -e), synced it to smbd secrets.tdb, wired `APPHUB_DRIVE_PW_*` + domain SID, restarted. **`gw-verify.sh` GREEN**: drive-password provision → HTTP 204; `smbclient` write as kriengkraip succeeded; **file owned `10000:100000`** (real LDAP uid, not 65534) — ownership-correct by construction. Self-serve UI exists (Settings → set drive password). MapDrive default already = gateway | flip (reversible) | drive-pw 204; SMB owner=10000; smbd+apphub active | revert `.env` `APPHUB_DRIVE_PW_*`; restart |
| 22 | 2026-06-28 | **all catalog images built + launching**: built on node1 (apptainer) → `python-apps.sif` 242M (pre-baked streamlit/gradio/fastapi — atomic swap, was 43M), `jupyterlab.sif` 667M (jupyterlab 4.6 + scanpy/anndata/leidenalg/harmonypy/umap), `rstudio.sif` 662M (rocker). **Runner fixes**: (a) **regression fix** — the pip-cache `--env` flag isn't supported by the older singularity on some compute nodes (broke launches that landed there) → switched to portable `SINGULARITYENV_`/`APPTAINERENV_` prefix; (b) bind host `/etc/passwd`+`/etc/group` ro so rserver can resolve the user; (c) rstudio rserver flags (`--server-user`, writable run dirs) | additive (NAS) | **live launch matrix (kriengkraip)**: static-html ✓ running, jupyterlab ✓ running, rstudio ✓ running (node2); test instances cleaned | restore prior `apphub-runner.sh` + `python-apps.sif` from `/mnt/sisplockers/.apphub-images/.build-*` n/a (atomic) |
| — | TODO | rstudio-seurat: add `Seurat` to `rstudio.sif` (a `%post: install.packages('Seurat')` — slow); gradio needs a real entry file in the locker. Optional: `nas.sisp.com`→192.168.0.25 DNS record (then switch MapDrive config server from the IP) | follow-up | n/a | n/a | (schema+pwsync bind+ACL+smb.conf+domain SID+enable drive-password), then: user sets drive password in AppHub → `smbclient //127.0.0.1/sisplockers -U user%pw` test owner → open 445 to LAN → `nas.sisp.com`→node1 → deploy repo web config (gateway default, Infortrend optional). Images: build via `apphub/images/build-images.sh` (atomic: temp -> move). (canonical design already in repo: per-user `%U` lockers, oplocks-off NFS coherence, SMB signing, vfs_fruit). Order: (0) **GATE** run `gateway/s1-ownership-spike.sh` live — if ownership doesn't round-trip, STOP; (1) NFS backhaul → `/srv/nas/sisplockers`; (2) `sssd.conf` (LDAPS+CA); (3) load Samba schema + `cn=samba-pwsync` bind+ACL + domain SID, enable `APPHUB_DRIVE_PW_*`; (4) `provision-lockers.sh`; (5) verify A-owns/B-can't + Finder; (6) point `nas.sisp.com`→gateway, deploy repo web config (`nas.sisp.com`). **node2 recommended** over node1 (control-plane blast radius). NOT done now: prod-LDAP schema/ACL surgery is the one cluster-wide-blast-radius step + needs the S1 gate first — do it watched, not at session tail | follow-up | n/a | n/a |
